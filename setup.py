from setuptools import setup, find_packages

setup(
    name="wheelgen",
    version="1.0",
    author="Your Name",
    description="Blender add-on for generating wheels",
    license="MIT",
    packages=find_packages(),
    include_package_data=True,
    install_requires=[],
)